1.把二元查找树转变成排序的双向链表
 题目：
输入一棵二元查找树，将该二元查找树转换成一个排序的双向链表。
要求不能创建任何新的结点，只调整指针的指向

struct Node{
int data;
struct Node *left;
struct Node *right;
}

struct Node *head = proot;
void doubleLinkTree(Node *root){
if(root->left == NULL)
	doubleLinkTree(root->left);
head = root;


}
2.

